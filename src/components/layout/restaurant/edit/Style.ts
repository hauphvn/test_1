import { makeStyles } from '@material-ui/core';

export const useEditStoreStyles = makeStyles(() => ({
    editStoreWrapper: {
        height: '100%',
        width: '100%',
    },
    title: {
			width: '215px',
    },
}));